# 🚀 התקנה מהירה - 3 דקות!

## קבצים שקיבלת:

1. ✅ **`gemini-prompt-custom-system.txt`** - הפרומפט המוכן (העתק ל-MAKE)
2. ✅ **`custom-status-rules.csv`** - טבלת כללים (Excel)
3. ✅ **`custom-status-decision-tree.md`** - הסבר מפורט

---

## צעדי התקנה:

### שלב 1: פתח את MAKE (30 שניות)
1. היכנס ל-Scenario שלך
2. מצא **Module 163** (Gemini AI - בחירת סטטוס)
3. לחץ עליו לעריכה

### שלב 2: החלף את הפרומפט (1 דקה)
1. פתח את `gemini-prompt-custom-system.txt`
2. **Ctrl+A** (בחר הכל)
3. **Ctrl+C** (העתק)
4. במודול MAKE:
   - מצא **"System Instructions"**
   - **מחק** את הפרומפט הישן כולו
   - **Ctrl+V** (הדבק את החדש)
5. **שמור!**

### שלב 3: בדיקה (1 דקה)
הרץ test עם אחד מהמיילים:

**Test 1: קליטת הצטרפות**
```
Input:
- תקציר: "בקשת ההצטרפות התקבלה בחברה"
- סטטוס נוכחי: "ממתין לפתיחת קופה"

Expected Output:
- selected_title: "ממתין להפקדה ראשונה"
- confidence: 0.98
```

**Test 2: נספח ו'**
```
Input:
- תקציר: "אישור נספח ו בגין בקשת ההצטרפות"
- סטטוס נוכחי: "ממתין לקבלת נספח ו'"

Expected Output:
- selected_title: "ממתין למשלוח נספח ו' למעסיק"
- confidence: 0.98
```

**Test 3: רג'קט**
```
Input:
- תקציר: "חסרים מסמכים"
- סטטוס נוכחי: כל סטטוס

Expected Output:
- selected_title: "רג'קט"
- confidence: 0.9
```

---

## ✅ זהו! סיימת!

המערכת עכשיו תזהה אוטומטית:
- ✅ קליטת הצטרפות → ממתין להפקדה ראשונה
- ✅ נספח ו' → ממתין למשלוח למעסיק
- ✅ הפקדה ראשונה → ממתין לשיוך כספים
- ✅ ניוד כספים → ממתין להשתקפות בשורנס
- ✅ חוסרים/דחייה/שגיאות → רג'קט

---

## 📝 איך להוסיף כלל חדש? (3 דקות)

### דוגמה: הוספת "קבלת פוליסה"

נניח שאתה רוצה:
- **מייל:** "הפוליסה הונפקה"
- **מ-סטטוס:** "ממתין לפוליסה"
- **ל-סטטוס:** "הסתיים טיפול"

### צעד 1: הוסף ל-CSV
פתח `custom-status-rules.csv` והוסף שורה:
```csv
פוליסה_הונפקה,"הפוליסה הונפקה|פוליסה מספר|הנפקת פוליסה",ממתין לפוליסה,הסתיים טיפול,פוליסה הונפקה בהצלחה,1.0
```

### צעד 2: הוסף לפרומפט
פתח `gemini-prompt-custom-system.txt`:

**א. הוסף בטבלה (Decision Matrix):**
```
| פוליסה_הונפקה | ממתין לפוליסה | הסתיים טיפול | פוליסה הונפקה |
```

**ב. הוסף מילות מפתח:**
```markdown
## ✅ פוליסה_הונפקה
**מילות מפתח:**
- "הפוליסה הונפקה"
- "פוליסה מספר"
- "הנפקת פוליסה"
```

### צעד 3: העתק מחדש ל-MAKE
העתק את כל הפרומפט המעודכן ל-Module 163.

**זהו!** 🎉

---

## 🎯 הכללים הקיימים:

| # | נושא | מילות מפתח | מתי תקף |
|---|------|------------|---------|
| 1 | אישור קליטה | "בקשת ההצטרפות התקבלה" | ממתין לפתיחת קופה |
| 2 | נספח ו' | "אישור נספח ו" | ממתין לקבלת נספח ו' |
| 3 | הפקדה ראשונה | "קליטת הפקדה ראשונה" | ממתין להפקדה ראשונה |
| 4 | ניוד כספים | "הצבירה הועברה" | ממתין לצבירה מניוד |
| 5 | חוסרים | "חסרים מסמכים" | כל סטטוס |
| 6 | דחייה | "נדחה" | כל סטטוס |
| 7 | שגיאה | "שגיאה\|תקלה" | כל סטטוס |
| 8 | תיקון | "נא לתקן" | כל סטטוס |

---

## 🐛 פתרון בעיות

### בעיה: Gemini לא מחזיר JSON
**פתרון:** וודא שב-Module 163:
- `Response Format` = `application/json`
- `Temperature` = `0.1` (נמוך)

### בעיה: הסטטוס שנבחר לא קיים
**פתרון:**
1. בדוק שהשם ב-`selected_title` **זהה בדיוק** לשם ב-Surense
2. שים לב לרווחים ואותיות (אפילו אות אחת שונה = שגיאה!)

### בעיה: Confidence נמוך (<0.7)
**פתרון:**
- בדוק את מילות המפתח - אולי צריך להוסיף וריאציות
- דוגמה: "נספח ו" vs "נספח ו'" vs "נספח 6"

### בעיה: לא מוצא כלל
**פתרון:**
- הפעל את ה-Fallback Logic
- הוסף את הכלל החסר לטבלה

---

## 📊 ציפיות:

| מדד | לפני | אחרי |
|-----|------|------|
| דיוק בחירת סטטוס | ~60% | ~95% |
| זמן טיפול ידני | 5 דק/מייל | 30 שנ/מייל |
| שגיאות | גבוה | נמוך מאוד |

---

## ❓ שאלות נפוצות

**ש: מה אם אני רוצה לשנות כלל קיים?**
ת: ערוך את ה-CSV, עדכן את הפרומפט, והעתק ל-MAKE.

**ש: איך אני יודע אם זה עובד?**
ת: בדוק את `confidence` - אם >= 0.9 זה מצוין!

**ש: האם צריך לעדכן משהו ב-Module 157/158/159?**
ת: לא! רק Module 163 (הפרומפט).

---

## 💡 טיפ Pro:

שמור את ה-CSV ב-Google Sheets ועדכן אותו משם.
כל פעם שמוסיפים כלל:
1. עדכן ב-Google Sheets
2. העתק את השורה החדשה
3. הוסף לפרומפט
4. העתק ל-MAKE

**זה הכל! בהצלחה! 🚀**
