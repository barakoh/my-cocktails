# 🚀 מדריך מהיר - שיפור Module 163 (Gemini Status Selector)

## 📦 מה קיבלת?

1. **`gemini-module-163-improved-prompt.txt`** - הפרומפט המשופר (מוכן להעתקה)
2. **`status-transition-rules.csv`** - טבלת כללים (ניתן לעריכה ב-Excel)
3. **`gemini-status-decision-tree.md`** - הסבר מפורט על הלוגיקה

---

## ⚡ הוראות התקנה (5 דקות)

### שלב 1: פתח את MAKE Blueprint
1. היכנס ל-MAKE scenario שלך
2. חפש את **Module 163** (Gemini AI - "בחירת סטטוס")
3. לחץ עליו לעריכה

### שלב 2: החלף את ה-System Instruction
1. במודול, מצא את השדה **"System Instructions"**
2. **מחק** את הפרומפט הנוכחי **כולו**
3. פתח את הקובץ `gemini-module-163-improved-prompt.txt`
4. **העתק הכל** (Ctrl+A, Ctrl+C)
5. **הדבק** ב-System Instructions ב-MAKE

### שלב 3: בדוק את ה-Input
וודא שה-Module מקבל:
- ✅ `{{56.summary}}` - תקציר המייל
- ✅ `{{158.text}}` - רשימת הסטטוסים הזמינים

אם חסר משהו - תקן את ה-Mapper.

### שלב 4: שמור ובדוק
1. שמור את ה-Scenario
2. הרץ בדיקה עם מייל לדוגמה
3. בדוק שהפלט הוא JSON תקין

---

## ✏️ איך להוסיף כללים חדשים?

### דרך 1: עריכה ב-CSV (מומלץ!)

1. פתח את `status-transition-rules.csv` ב-Excel/Google Sheets
2. הוסף שורה חדשה:

| נושא_מייל | מילות_מפתח | סטטוס_נוכחי | סטטוס_מוצע | סיבה | confidence |
|-----------|-------------|-------------|------------|------|-----------|
| המייל_החדש | "מילה1\|מילה2\|מילה3" | הסטטוס הנוכחי | הסטטוס המוצע | הסבר קצר | 0.95 |

3. שמור את הקובץ
4. **העתק את השורה החדשה** לטבלה בתוך הפרומפט (בין שורות ה-`|---|`).

### דרך 2: עריכה ישירה בפרומפט

1. פתח את `gemini-module-163-improved-prompt.txt`
2. חפש את הסקציה **"Decision Matrix"**
3. הוסף שורה חדשה לטבלה:
```
| נושא_חדש | סטטוס_נוכחי | סטטוס_מוצע | סיבה |
```
4. הוסף את מילות המפתח בסקציה **"זיהוי נושא המייל"**
5. שמור והעתק מחדש ל-MAKE

---

## 🧪 דוגמה: הוספת כלל חדש

**רוצה להוסיף:** "כשמגיע מייל עם בקשה לעדכון פרטים, תעביר ל'ממתין לעדכון לקוח'"

### צעד 1: הוסף לטבלה
```
| עדכון_פרטים | ממתין לאישור | ממתין לעדכון לקוח | צריך עדכון פרטים |
```

### צעד 2: הוסף מילות מפתח
```markdown
**עדכון_פרטים:**
- "עדכון פרטים", "שינוי כתובת", "עדכון טלפון", "עדכון דוא\"ל"
```

### צעד 3: העתק לפרומפט ושמור

זהו! עכשיו המערכת תזהה מיילים של עדכון פרטים ותמליץ על הסטטוס הנכון.

---

## 📊 בדיקה ותחזוקה

### איך לבדוק שהפרומפט עובד?

1. **הרץ test** עם מייל ידוע
2. בדוק את ה-Output:
   - ✅ `confidence >= 0.9` - מעולה!
   - ⚠️ `confidence 0.6-0.9` - סביר, אבל אולי כדאי להוסיף כלל
   - ❌ `confidence < 0.6` - הכלל לא קיים, הוסף אותו!

### תחזוקה חודשית (מומלץ)

1. **סקור את המיילים שקיבלו `confidence < 0.8`**
2. **זהה דפוסים חוזרים**
3. **הוסף כללים חדשים לטבלה**

---

## 🎯 טיפים לכתיבת כללים טובים

### ✅ DO:
1. **השתמש במילות מפתח מגוונות**
   - טוב: "טופס חתום|סיום חתימה|חתימה מרחוק"
   - רע: "טופס חתום"

2. **הוסף סיבה ברורה**
   - טוב: "טפסים מוכנים לשליחה ליצרן"
   - רע: "סיום תהליך"

3. **קבע confidence לפי ודאות**
   - 1.0 - ודאות מלאה (ביטול, פוליסה הונפקה)
   - 0.95 - כמעט בטוח (אישור יצרן)
   - 0.8-0.9 - סביר (שגיאות, שינויים)
   - 0.6-0.7 - לא בטוח (השתמש רק ל-Fallback)

### ❌ DON'T:
1. **אל תשתמש במילים כלליות מדי**
   - רע: "מייל|הודעה|פנייה" ← יתפוס הכל!

2. **אל תשכח לעדכן את שתי הסקציות**
   - Decision Matrix (הטבלה)
   - זיהוי נושא (מילות מפתח)

3. **אל תיצור חוסרי התאמה**
   - אם בטבלה כתבת `סטטוס_מוצע: "ממתין לשליחה"`,
   - ודא שהסטטוס הזה **באמת קיים** ברשימת הסטטוסים!

---

## 🐛 פתרון בעיות

### בעיה 1: הפרומפט לא מחזיר JSON
**פתרון:**
- בדוק ש-`Response Format` ב-Module 163 מוגדר ל-`application/json`

### בעיה 2: הסטטוס שנבחר לא קיים
**פתרון:**
- Module 157 מושך את כל הסטטוסים
- וודא שהסטטוס_מוצע בטבלה **תואם בדיוק** לשם בסטטוסים

### בעיה 3: Confidence תמיד נמוך
**פתרון:**
- בדוק את מילות המפתח - אולי הן לא תופסות את המיילים
- הוסף וריאציות נוספות (רבים/יחיד, עם/בלי ניקוד)

### בעיה 4: לא מוצא כלל כלל
**פתרון:**
- הפעל את ה-Fallback Logic
- בדוק אם יש טעויות כתיב בטבלה

---

## 📈 שיפורים עתידיים (אופציונלי)

### רמה 1: הוסף קטגוריות
במקום לבדוק כל נושא בנפרד, קבץ לקטגוריות:
- תביעות: אישור/דחייה
- מינוי: טפסים/אישורים
- פדיונות: בקשה/אישור

### רמה 2: מעברים מרובים
אפשר למודול להציע **מספר סטטוסים** (לא רק אחד):
```json
{
  "primary_suggestion": {...},
  "alternative_suggestions": [...]
}
```

### רמה 3: למידה אוטומטית
שמור מיילים עם ה-confidence שלהם ב-Google Sheets.
אחרי חודש - נתח ושפר את הכללים.

---

## 🎓 סיכום

✅ **העתקת את הפרומפט המשופר** ← Module 163
✅ **הוספת כללים חדשים** ← CSV או ישירות בפרומפט
✅ **בדיקה ותחזוקה** ← חודשית

**זמן יישום כולל: 5-10 דקות**
**שיפור דיוק: עד 40%**

---

## 💡 זקוק לעזרה?

- **CSV לא נפתח?** נסה Google Sheets
- **כלל לא עובד?** בדוק ש-UUID בסטטוסים תואם
- **רוצה לבדוק פרומפט?** הרץ ב-Gemini Playground קודם

**בהצלחה! 🚀**
